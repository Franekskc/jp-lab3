import java.awt.*;

public class MyShape {
    protected int x,y;
    protected Color color;
    public MyShape(int x, int y, Color color){
       this.x = x;
       this.y = y;
       this.color = color;
    }
}
